## Building and running unit tests

To build and run unit tests, use the following commands from the main repo library:
```
cd buildVCPkg
make
cd utests
./utest_vec3 #or whichever unit test you want

```